# SOVRN Growth — Roofers Landing Page
Static single-file landing page for sovrngrowth.com.

- `index.html` — the entire page (no build step required)
- `assets/hero-roofs.jpg` — cinematic hero background (add this file; page degrades gracefully without it)

Deploy: connect this repo to Vercel as a static project, root directory `/`, no build command.
